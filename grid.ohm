grid {
  Program = Statement+
  Statement=
    | EOL -- empty
    | "Return" Expr EOL -- return

  Expr =
    | plainstring -- plainstring

  plainstring = "\"" (~"\"" any)* "\""

  EOL = comment? nl

  nl = "⎩" digit+ "⎭"
  comment = "⎝" (~"⎠" any)* "⎠"
}
